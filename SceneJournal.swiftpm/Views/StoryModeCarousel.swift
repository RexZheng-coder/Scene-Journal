import SwiftUI

// Placeholder file to satisfy stale Xcode file references.
// Story Mode feature has been removed from ContentView.
struct StoryModeCarousel: View {
    let entries: [JournalEntry]

    var body: some View {
        EmptyView()
    }
}
